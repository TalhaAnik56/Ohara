import { Button } from "@chakra-ui/react";

const SignIn = () => {
  return (
    <Button
      size={{ base: "sm", lg: "md" }}
      variant={"outline"}
      colorScheme="blue"
    >
      Sign In
    </Button>
  );
};

export default SignIn;
